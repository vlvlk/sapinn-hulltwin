# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Data (large / private). The Shifts upload itself is NOT committed (train.csv
# is ~109 MB > GitHub's 100 MB limit); the small metadata files are tracked,
# and scripts/download_shifts.py fetches the full archive from Zenodo.
data/external/
data/logs/
*.parquet
*.nc
*.zip

# Notebooks
.ipynb_checkpoints/

# Environment
.env
